/compound-engineering:brainstorming 我想做一款去中心化的IM，我准备分peer(中转,stun),store(存储信息和提供信息，可以接收远程的消息和中转手机发送的消息，手机可以直接从store中获取信息搜索信息和发送信息)，或者用
户也可以简单的使用store和手机程序融合的版本，接着创建身份什么的依靠像gpg那样的生成私钥公钥，交换信息就签名，然后生成sha值后继续，之后支持群聊功能，群聊需要E2EE，中转可以进行临时的存储，这样还可以制作像频道那样的公开消息，
然后再弄一个信息交换端，管理是否接收信息，是否提供信息给其他发送者，还可以多个模式，如果是接收未加密非公钥验证的信息的话，那可以随便接收消息，弄个公开的，看看这个方法怎么样？还有没有可以优化的点？