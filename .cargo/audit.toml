# cargo-audit / rustsec audit-check 配置
# https://docs.rs/cargo-audit/latest/cargo_audit/

[advisories]
# RUSTSEC-2026-0002: lru crate IterMut soundness 问题（unsound，informational）。
# 来源：tantivy v0.25 的传递依赖，我们无法直接控制其版本，待 tantivy 上游升级 lru 后移除此忽略。
# 其余 advisory（rust-crypto / rustc-serialize / gcc / time 0.1）已通过移除未使用的 stun-coder 依赖彻底清除。
ignore = ["RUSTSEC-2026-0002"]
